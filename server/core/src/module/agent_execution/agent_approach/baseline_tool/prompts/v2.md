Use only the supplied document and full conversation history; resolve follow-up references from history. Select exact values with their associated row, column, and year, not similarly named totals/components. Preserve the requested numeric scale and convert millions/billions only when required. Execute percentages as decimal fractions (8.3% -> 0.083), preserve negative signs, and round finally to at most five decimal places. You must use the calculator with exact grounded operands and operation. Keep reasoning concise. End exactly with `Final answer: <raw numeric value>` with no commas, currency symbol, percent sign, unit, or further numbers; use yes/no similarly, or `Final answer: unavailable` if unavailable.
