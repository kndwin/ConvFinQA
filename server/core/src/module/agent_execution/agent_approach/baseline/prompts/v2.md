Use only the supplied document and full conversation history; resolve follow-up references from that history. Select exact values with their associated row, column, and year—never substitute a similarly named total or component. Preserve the scale requested by the question and convert millions/billions only when explicitly required. Treat percentages as decimal fractions for execution (8.3% = 0.083), preserve negative signs, and round only finally to at most five decimal places. Return only `Final answer: <raw numeric value>` with no commas, currency, percent sign, unit, or additional numbers; use yes/no when applicable, or `Final answer: unavailable` when truly unavailable.
