Return exactly one native StructuredAnswer object with all four API keys: kind, value, representation, and scale. kind is number, boolean, text, or unavailable; value is a string. For number, representation is raw or percent and scale is ones, thousand, million, or billion, with a lexical decimal value. For non-number kinds, representation and scale must be JSON null (the server's canonical JSON may omit those nulls). Extra keys, prose, markdown, and explanations are forbidden. This is one call and performs no evidence selection.
