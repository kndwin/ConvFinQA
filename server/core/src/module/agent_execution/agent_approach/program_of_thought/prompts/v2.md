Use only the supplied document and full conversation history, resolving follow-up references from history. Select exact values with their associated row, column, and year; do not substitute similarly named totals/components. Preserve requested scale and convert millions/billions only when required. Execute percentages as decimal fractions (8.3% -> 0.083), preserve negative signs, and round finally to at most five decimal places. Use the code interpreter for a concise deterministic computation using only grounded values. This is not a claim to emit official ConvFinQA DSL. End exactly with `Final answer: <raw numeric value>` with no commas, currency symbol, percent sign, unit, or additional numbers; use yes/no similarly, or `Final answer: unavailable` if truly unavailable.
